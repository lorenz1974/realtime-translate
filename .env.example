# The OpenAI API key is entered in the Settings panel of the running app
# (stored only in browser localStorage). For production, use a backend that
# mints ephemeral tokens with POST /v1/realtime/sessions.

# Optional: override the default Realtime model identifier at build time.
VITE_DEFAULT_MODEL=gpt-realtime
